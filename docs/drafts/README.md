# Entry drafts (delete each script once its entry is appended)

Ordering (operator-approved 2026-09-01, amended by the 0017 correction found 2026-09-02):
**0017** correction (`append_0017.py <date>`: composio adapter shape defect + headroom `paid`
mis-definition; supersedes the FIGURES of 0013 and 0015's ratio, verdicts unchanged) →
**0018** the corrected figures, written only from a fresh `e7` + `summarize_e7` run after 0017
is committed (no script yet; it is written from that output the way 0015 was) → **0019** E9
registration (`append_0019.py <date>`, band approved). Each script refuses to run out of order
and runs `ledger_check` after appending. Run from the repo root with `.venv/Scripts/python.exe`.
