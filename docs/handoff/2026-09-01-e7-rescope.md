# Handoff — program re-scope to the E7 measurement lane

**Date:** 2026-09-01
**Describes commit:** the working tree after the 0007 review-amendment set (see the commit
block accompanying it; the prior landed state is `1f7ad90` -- entry 0006, depth summarizer,
banner updates -- operator-verified by independent clone: 114 pass, lints clean).
**Written by:** the session that drafted entries 0006/0007 and the review fixes, at the time
of writing them.

## Current state

- **Screen line CLOSED** (entry 0006, operator decision 2026-09-01). E0's SAME verdict stands
  under the frozen rule; entry 0003 untouched. H-S1/H-S3/H-S4 carry `SHELVED` (entry 0007's
  vocabulary addition); H-S2 first clause `NOT CONFIRMED`; only H-E7a/H-E7b are live.
- **Depth structure recorded** in entry 0006 `[BASELINE]`, recomputed by the new fail-closed
  `summarize_e0_depth` (inherits summarize_e0's checks; cross-lambda exceedance identity is
  checked, not asserted). Both readings -- real end-of-network separation vs proxy artifact --
  deliberately left open; nothing in this program will decide them.
- **E7 promoted** to a standalone measurement program: taxonomy + switch-point headroom +
  compaction break-even, on public agent traces, at pinned pricing. Anchor venue MLSys 2027
  (due 2026-10-30). Optional LCFM @ NeurIPS 2026 4-pager (due 2026-09-10 AoE, non-archival,
  dual-submission permitted) gated by the day-2 gate.
- **Registered rules that bind the build** (read entries 0006 AND 0007 whole before writing
  parser code): H-E7a decided by Lane A ALONE, Lane B descriptive-only; coverage floor >= 50
  trajectories AND >= 3 distinct agents per suite, >= 2 suites; cost model = Anthropic 5-min
  TTL base case, idle-gap expiry from trace timestamps, two-bound H-E7b where timestamps are
  absent; no third lane or new policy without a new numbered entry pre-replay; replay refuses
  until the registering entries are committed and unmodified (enforcement lands with the
  harness -- it does not exist yet).
- **E7 has ZERO code.** No parser, no replay skeleton, no cost model, no E7 summarizer. The
  tree's only instrument is the closed screen's, plus its two summarizers.

re-verify:
- `.venv/Scripts/python.exe -m pytest -q` -> expect 118 passed (114 + 4 chain/vocabulary tests; count measured, not derived)
- `.venv/Scripts/python.exe -m linear_ceiling.ledger_check` -> `ledger ok` (now includes the
  prior-entries-sha256 chain from entry 0007 on)
- `.venv/Scripts/python.exe -m linear_ceiling.lint_scope` -> `scope ok`
- `.venv/Scripts/python.exe -m linear_ceiling.summarize_e0_depth` -> exit 0, verdict.json
  sha256 59fd962f92788eb4323594226e053ce121fe4ba17a1e821af0f9a43409e0c3bf
- entry count: 7 entries (0001-0007); H-S1/H-S3/H-S4 read `SHELVED` in the table

## Locked decisions (with their stated reasons)

- **Screen line closed for opportunity cost** (mechanism lane crowded/top-tier, measurement
  lane open) -- evidence in docs/2026-09-01-measurement-lane-evidence.md. Reopening any H-S
  hypothesis is a numbered entry moving it back to `unresolved`.
- **Lane A alone decides H-E7a** (0007): Lane B measures the policy, not the workload.
- **Upstream drift** (kv-transfer-replication ahead of pin + dirty): operator ruled it a
  ONE-TIME exception (2026-08-31) -- do not build process around it; any upstream invocation
  records the actual upstream state it ran against.
- **MLSys 2027 is the anchor; LCFM is optional feedback**, never the destination. ICLR 2027
  was rejected (deadline collision + genre mismatch).

## Deadlines

| date | what |
|---|---|
| 2026-09-03 EOD | day-2 gate: 1 suite parsed, replay skeleton on >= 10 trajectories, both lanes in the parser -- miss => skip LCFM |
| 2026-09-08 EOD | numbers-freeze: only summarizer-recomputed numbers may enter the 4-pager |
| 2026-09-10 AoE | LCFM deadline (only if both gates passed) |
| ~2026-09-25 | AAAI-27 workshop list lands; check fit + per-workshop archival/dual-submission terms |
| 2026-10-30 | MLSys 2027 submission deadline (the commitment) |

## Open / next

1. Day-2 gate work: acquire one suite's trajectories (tau-bench `historical_trajectories` is
   the lowest-friction start; SWE-bench trajectories live in submitters' repos), build the
   parser + per-trajectory token/cost timeline, implement Lanes A and B.
2. The E7 harness must implement the assert_ready-equivalent against entries 0006/0007 before
   its first replay (requirement registered in 0006; unenforced until built).
3. Full texts of the four delta papers are read and tabled; re-check the arXiv listings once
   more in the week before the MLSys submission (the lane moves monthly).

## Known residual limitations

- The ledger entry chain (0007) protects registered entry text only; header and hypotheses
  table are editable commentary, and a history rewrite that regenerates the chain is locally
  undetectable -- same class as the seal's stated limitation.
- Lane B's cascade policy is one point in policy space, chosen pre-registration; its headroom
  numbers generalize only as far as the policy is representative, which the paper must say.
- The pricing pins are a snapshot (2026-09-01); providers reprice. A replay under different
  pricing re-pins via a successor entry (registered in 0006).
