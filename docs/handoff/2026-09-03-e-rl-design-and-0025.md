# Handoff — E-RL design (unregistered) and E9's pre-prefill amendment 0025

2026-09-03 · newest commit described: `5960c20` = `origin/main` (0 ahead / 0 behind at write time;
working tree clean apart from the untracked `.claude/`). Upstream `../kv-transfer-replication` at the
0023 pin `36d73b3f`, dirty on four Run 8 files that E9 does not invoke.

## Current state

- **built** — E9 entry 0025 on the record (`38af9c8`), instrument in `01368b8`, script that produced
  it in `7213136`, retired in `07af0d6`. All descriptive; the H-E9 rule, τ_K = 0.3186, the band and the
  cells are untouched. Registers: coverage 25 of 68 with an included-vs-excluded comparison from 0018's
  rows; τ_agent_K = 1 − 0020 arm (b) K R² = 0.4371 alongside (K arms); the τ ladder [0.10, 0.03]; a
  prefix-invariance control that HALTS (S vs S + R's first token, max centered δ ≤ 1e-4); causal seam
  distance b⁻(t); matched-block lengths and f* over blocks ≥ 4; a seeded bootstrap of the median
  (reported, not read); the δ_null equal-token fraction; `e9 --align-only`; keep-subset n 3 → 8.
  re-verify: `awk '/^### 0025/,/^### 0026/' ledger/ledger.md | head -3 && .venv/Scripts/python.exe -m linear_ceiling.ledger_check && .venv/Scripts/python.exe -m linear_ceiling.e9 --check` (expect the heading, `ledger ok`, `E9 gate: ready (entries 0019, 0023 and 0025 …)`).
- **built** — suite 380 passed / 1 skipped at `5960c20`, `lint_scope` and `seal verify` green.
  re-verify: `.venv/Scripts/python.exe -m pytest -q` (expect `380 passed, 1 skipped`).
- **built** — `results/e9/calibration/tau.json` carries `agent_K` (rewritten by `--calibrate-tau`,
  ~2 min CPU, 2026-09-02); local by rule, the summarizer refuses without it.
  re-verify: `python -c "import json; print(json.load(open('results/e9/calibration/tau.json'))['tau']['agent_K'])"` (expect 0.4371020133925453; if the file is absent, run `summarize_e9 --calibrate-tau`).
- **built** — `results/e9/align/coverage.json` from `e9 --align-only` on the real corpus (25/68;
  155,257 matched tokens, 0.44 % in length-1 blocks, 99.01 % in blocks ≥ 4). Local by rule.
  re-verify: `.venv/Scripts/python.exe -m linear_ceiling.e9 --align-only` (16 s CPU; expect `coverage.json` with observed 68 / included 25).
- **built** — README prose and two mermaid diagrams updated for 0025 and the E-RL line (`5960c20`);
  CLAUDE.md program state says "0025 appended"; drafts README says "no drafts pending".
  re-verify: `grep -n 'E-RL' README.md | head -3 && grep -c 'no drafts pending' docs/drafts/README.md`.
- **planned, design only** — E-RL: KV reuse across RL post-training checkpoints, reframed 2026-09-02 as
  an MLSys recompute-cost vs stale-KV-cost comparison on the operator's ruling ("I'm not doing
  RL-algorithms"). `docs/2026-09-02-e-rl-design.md` (commits `108c60f`, `dbf557a`). Unregistered,
  unnumbered, no code, no config. Its §10 is a refutation record; every citation is pinned at source
  (AReaL §4.1, Laminar §2.3, vLLM async-RL docs, Stable Asynchrony body, LlamaRL §6).
  re-verify: `grep -c '???' docs/2026-09-02-e-rl-design.md` (expect > 0: open items remain) and `grep -cE '^### .*E-RL|^\| H-RL|E-RL registered' ledger/ledger.md` (expect 0: 0025 mentions the design by name, but no entry, hypothesis row or registration exists).
- **not started** — E9 GPU run (A100 not requested; runbook `docs/2026-09-02-e9-gpu-runbook.md`,
  pre-flighted by the parallel session in `c7f59dd`). E-RL's first build step (revision-aware `Pair`
  upstream). The H-E9 verdict entry (unnumbered until staged).

## Locked decisions

- **E-RL is MLSys, not RL-algorithms** (operator, 2026-09-02). Reason: the natural comparator under the
  AReaL/Laminar/vLLM framing is a systems trade-off, and the algorithms-track version would require
  two training runs the compute cannot fund. Design §1.
- **τ_K stays the verdict-bearing τ for both E9 and E-RL; nothing is recalibrated** (0023, carried into
  the design §2). Reason: a τ moved between experiments destroys the one-yardstick comparison and the
  recalibration defect (mapper failing its own calibration at the median) is on the 0023 record.
  τ_agent_K is alongside only.
- **Statistic (B), the stale-vs-fresh importance ratio + ESS, is verdict-bearing for E-RL by named
  amendment** (author Hossain Pazooki, 2026-09-02; design header and §2). Reason: the seed's own rule
  in A.3(b) says promotion is a named amendment, never a silent edit. Its bounds are still `???` and
  will read "operator's stated judgment" — no cited paper carries a numeric ESS threshold (checked in
  the Stable Asynchrony body).
- **Own run primary and unconditional; OLMo the descriptive far tail** (design §3). Reason: an engine
  decides at lag 1–8 policy versions and only per-step checkpoints reach that regime; OLMo's finest lag
  is 200.
- **Entry numbers are provisional until a script is staged; `docs/drafts/README.md` is the only
  allocator** (decided 2026-09-02, unchanged). E-RL has no number.
- **0025 registers descriptive quantities only; the band reads one point median at τ_K** (entry text).
  Reason: 0023's "nothing in the rule section is revisited after the first score file" plus the
  refusal to pick a looser τ after seeing it is looser.
- **Keep subset n = 8 by a fresh draw, not the old 3 plus 5** (entry 0025). Reason: numpy's choice
  without replacement is not nested across sizes, and nothing had been prefilled. 48.2 GB retained.

## Reuse map

- `docs/2026-09-02-e-rl-design.md` — the whole E-RL design with the τ seed as Appendix A and the
  refutation record in §10; §8 is the list of rulings still needed.
- `src/linear_ceiling/e9_pertoken.py` — `seam_distance_left`, `block_lengths`, `block_bin`,
  `bootstrap_median_interval` (0025), beside 0023's `centered_delta`, `f_star`, `seam_distance`,
  `null_pairs`. E-RL's per-token statistics are these functions on a different pair.
- `src/linear_ceiling/e9.py` — `prefix_invariance_max_delta` (shared by driver halt and summarizer
  recheck), `align_only`, `run_controls` with the S+1 dump; `keep_subset`.
- `src/linear_ceiling/e9_align.py::coverage_comparison` — included-vs-excluded on |S|, |R| and 0018
  rows, keyed `<traj_id>#<switch_index>`; used by the summarizer and by the (retired) append script.
- `src/linear_ceiling/summarize_e9.py` — `calibrate_tau` now writes `tau.agent_K` from
  `results/e8/report.json` arm (b); `_check_calibration` refuses drift on it.
- `git show 7213136:docs/drafts/append_0025.py` — the script that produced 0025: the ordering-guarded
  shape for the next amendment (prior values from a PINNED revision, `e7-manifest-sha256:` line when
  0018 rows are quoted, `REQUIRED_ENTRIES` agreement check, no-prefill guard).
- `tests/test_e9.py::fake_runner_factory` — `prefix_nonzero` flag; `write_fake_e7_report` in
  `tests/test_summarize_e9.py` for the coverage comparison.
- Upstream primitives for E-RL's (B) scorer: `kvt/cache.py::build_cache`, `forward_with_cache`
  (prefix case only; E9's interleaved case needs per-layer substitution — not built).

## Invariants

- `e9.assert_ready` requires `### 0019`, `### 0023`, `### 0025` in the COMMITTED ledger and
  `config/e9.toml` committed unmodified; any config edit (even a comment) turns the gate red until
  committed.
- `load_e9_config` refuses: `tau_ladder` not strictly decreasing inside (0, τ_K); `tau_agent_K` outside
  (τ_K, 1); `min_block_len` < 1; `prefix_invariance_max_delta` outside (0, 1); `bootstrap_reps` < 100.
- The driver HALTS on the prefix-invariance control (max centered δ > 1e-4) before scoring any handoff;
  the summarizer refuses a prefix record that is absent, tampered, over tolerance, or whose extra token
  is not R's first.
- `summarize_e9` refuses without `results/e7/skeleton_report.json` (coverage comparison) and without
  `agent_K` in `tau.json` equal to config and to 1 − E8 arm (b) K R².
- Any ledger entry ≥ 0024 whose text contains `summarize_e7` must end with `e7-manifest-sha256: <committed manifest sha>` — including E9 entries that quote 0018 rows.
- A draft script's "prior value" comes from a pinned revision, never HEAD; every emitted commit block
  chains a refusable step to the destructive step after it with `&&` (learnings 2026-09-02, the
  retire-before-append slip at `cb80ad0`).
- E-RL: never write "lag 20" for OLMo (stride 200); `main` is not a lag point; τ is inherited, never
  recalibrated; nothing is registered or numbered until the §8 rulings land.
- `traces/` and `results/` are local by rule; the GPU runbook's keep-handoff names predate 0025 and are
  superseded by the entry's list.

## Open / next

1. **The A100 request** (human-only) — E9 is READY on the committed state; the runbook's memory finding
   (exclusive 40 GB card; float32 + full logits) stands.
2. **E-RL rulings before anything is built** (design §8): (B)'s bounds as "operator's stated
   judgment"; the three cell-table gaps in §2; trainer choice and pin for the own run; one H-id or two;
   where the (B) scorer lives; lag-0 registration of the DPO base; "step" unit for OLMo (open-instruct
   has no 1B script); ladder values, window, prompt set, seal numbers.
3. **Upstream change request** once ruled: revision-aware `Pair` plus a local-path pair, on a branch
   (the upstream tree is dirty with Run 8 WP3). E9 stays on `36d73b3f`.
4. Housekeeping the other session owns: the runbook's keep names/volumes; brief v1 and the adversarial
   PDF still say 68 and "< 1 h".
