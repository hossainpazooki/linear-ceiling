# Handoff — E8 amendment (entry 0030): arm (b) over every agent sequence, awaiting the operator's re-pin commit

2026-09-05 02:20 UTC. Newest commit this brief describes: linear-ceiling `31a5f69` (H-E9 HELD landed) with the
amendment UNCOMMITTED in the working tree; upstream `kv-transfer-replication` at `d5786df` with the amendment's
change uncommitted there too. Pick-up measures drift from `31a5f69` / `d5786df` and from the file list below.

## Current state

- **built / verified** — upstream: `scripts/score_mapper.py` accepts `--holdout-frac 1.0` when only scoring (train
  figures null; empty held-out still refused) and the `--per-token` record carries `seq_idx`, `seq_ids`,
  `sse_seq_*`, `sst_seq_*` plus a `per_sequence` json block; `kvt/pertoken.py::per_sequence_moments` (SST around the
  global held-out mean, sums reproduce the layer moments). Two new tests.
  re-verify: `cd ../kv-transfer-replication && .venv/Scripts/python.exe -m pytest -q` → `145 passed`; `git status --short kvt scripts tests` shows the three files modified (or, after the operator's commit, `git log -1 --stat` names them).
- **built / verified** — linear-ceiling: `config/e8a.toml` (separate `results/e8a/`; `agent_holdout_frac = 1.0`;
  `reuse_agent_dumps_from = results/e8/report.json`; `[e8.amendment]` entry 0030, seed 30, 2000 reps); `E8Config`
  optional fields + loader checks; `e8.reuse_agent_dumps` (fingerprint + token-file check), `e8.required_entries`
  (gate adds 0030), `score(..., holdout_frac, per_token)`, `assemble` amendment rows; `summarize_e8`
  `_amendment_figures` (per-sequence recompute checked against report AND re-scored json, seeded bootstrap of arm (b)
  and of the drop, change from 0020, prior report hash re-checked) + `summary.json`; tests for all of it.
  re-verify: `.venv/Scripts/python.exe -m pytest -q` → `388 passed, 1 skipped`; `.venv/Scripts/python.exe -m linear_ceiling.lint_scope` → `scope ok`.
- **built / verified** — ledger entry **0030** appended (registration, descriptive, no verdict line; names the 0020
  dumps by fingerprint and the prior report sha; H-E8 cell and τ_agent_K explicitly unmoved).
  re-verify: `.venv/Scripts/python.exe -m linear_ceiling.ledger_check` → `ledger ok`; `grep -n "^### 0030" ledger/ledger.md` → one line at 1822.
- **built** — `docs/drafts/append_0031.py` (figures entry; runs `summarize_e8 --config config/e8a.toml` in-process).
  re-verify: `.venv/Scripts/python.exe -m py_compile docs/drafts/append_0031.py` exits 0; it REFUSES until 0030 is committed, the pin recorded and `results/e8a/report.json` exists.
- **in progress (blocked on the operator)** — the upstream commit, the three-sed re-pin, the linear-ceiling
  registration commit (block in the 2026-09-04 session log; repeated in *Open / next*).
- **planned** — the rescoring run (`e8 --config config/e8a.toml`, CPU ≈ 10 min, no dumps), `summarize_e8` on it,
  `append_0031.py`, then retire both append scripts and update the drafts README's current-state line.
- **scratch preview, NOT a ledger figure** — smoke of the new scorer on 0020's agent dumps at k = 1 with
  `--holdout-frac 1.0`: 50 sequences / 12,800 tokens, per-sequence sums reproduce the pooled R² to 1e-12. The R²
  values it printed are not cited anywhere and must not be: 0031 takes them from the summarizer.
  re-verify: none needed — the claim is that no such number appears in `ledger/ledger.md` (`grep -c "0.5708" ledger/ledger.md` → 0).

## Locked decisions

- **Arm (a) keeps `holdout_frac` 0.2; only arm (b) goes to 1.0.** Reason: the k = 1 mapper was fit on the generic
  calibration dumps (arm (a)'s training rows); scoring all of them is in-sample. Agent text was never fit on. (0030,
  "What is registered".)
- **Descriptive only: the H-E8 cell (0020, NOT CONFIRMED) and τ_agent_K (0025, 1 − 0.5629) do not move.** Reason:
  the cell was decided under the registered 0016 protocol; τ_agent_K has already been read by 0029; substituting
  either after the fact is post hoc by the ledger's own rule. The all-sequence figures are reported beside them.
- **Separate results dir `results/e8a/`, 0020's dumps reused by fingerprint, nothing re-dumped or resampled.**
  Reason: `summarize_e9 --calibrate-tau` reads `results/e8/report.json` and refuses on disagreement; rewriting it would
  break E9's own gate. Reuse-by-fingerprint makes 0031 a rescoring of 0020's tensors, not a new sample.
- **Two entries, not one.** Reason: 0030 registers before any score exists (its script refuses if `results/e8a/`
  holds a report); 0031 carries numbers and therefore is appended by a script that runs the summarizer in-process
  (`docs/drafts/README.md` convention).
- **E8's original pin `71df4504` is left as is; its gate is known to refuse by drift.** Reason: 0020 stands on its
  record; repairing a closed experiment's gate is a new entry's job, not a silent edit. Recorded in 0030 and in the
  2026-09-05 learnings entry.
- **Re-pin bookkeeping is three seds in one commit** (new sha into `config/e8a.toml` + `UPSTREAM.md`, parent sha to 8
  chars, `linear_ceiling.UPSTREAM_SHA` bumped). Reason: `test_imports` requires exactly one full sha equal to the
  constant; 0026 failed CI on this (2026-09-04 learnings entry).

## Reuse map

- Append-script shape: `docs/drafts/append_0030.py` (parameters from config + prior record) and `append_0031.py`
  (figures from an in-process summarizer), both ending in `ledger_check`; retire after appending.
- Fingerprint reuse: `e8.reuse_agent_dumps` and `e8.dump_fingerprint`; the same shape `summarize_e9` uses for kept dumps.
- Quantiles only via `linear_ceiling.e7_stats.quantile`; RNG only via `linear_ceiling.rng.make_rng` (the bootstrap
  seeds `seed + k` per k).
- Per-sequence arithmetic: upstream `kvt.pertoken.per_sequence_moments`; downstream `summarize_e8._seq_r2` /
  `_pooled_from_seqs` (1 − Σsse/Σsst per head, then mean).
- Prior E8 record: `results/e8/report.json` (0020; fingerprints, per_k), `results/e8/r2/agent_k*.json` (n_seqs,
  holdout_frac, token counts) — read, never written.

## Invariants

- `results/e8/` is never rewritten (E9's τ calibration reads it). The amendment writes only under `results/e8a/`.
- `e8 --config config/e8a.toml` refuses until: `ledger/ledger.md` with 0009 + 0016 + 0030 and `config/e8a.toml` are
  committed unmodified; `upstream_sha` is a real sha that is an ancestor of upstream HEAD with the invoked paths
  unchanged; the agent dumps and token file match 0020's fingerprints byte for byte; the mapper artifacts exist.
- No number enters the ledger that `summarize_e8 --config config/e8a.toml` did not produce; the smoke figures in this
  session's scratch are previews and are not cited.
- `results/`, `data/`, `traces/` never enter history. The 0020 agent dumps (2.8 GB) and token file exist only at home.
- `UPSTREAM.md` holds exactly one full sha, equal to `linear_ceiling.UPSTREAM_SHA`.

## Open / next

1. **Operator:** run the commit block from the 2026-09-04 session log (upstream commit → push → `SHA=$(git rev-parse
   HEAD)` → the three seds → `pytest tests/test_imports.py` → linear-ceiling commit → push). Add to that commit's
   `git add`: `docs/handoff/2026-09-05-e8-amendment-0030.md docs/handoff/HANDOFF.md docs/learnings/LEARNINGS.md
   docs/learnings/2026-09-04-every-re-pin-trips-the-one-full-sha-rule.md
   docs/learnings/2026-09-05-an-experiments-gate-dies-with-later-re-pins.md`. CI must be green.
2. Then, in order: `.venv/Scripts/python.exe -m linear_ceiling.e8 --check --config config/e8a.toml` (must print ready
   naming 0009/0016/0030) → `e8 --config config/e8a.toml` → `summarize_e8 --config config/e8a.toml` (paste a refusal
   verbatim; it is a finding) → `docs/drafts/append_0031.py` → retire `append_0030.py` and `append_0031.py`, update
   `docs/drafts/README.md` "Current state", commit, push.
3. Blocker: none technical. The gate refusal you see right now (`e8a.toml upstream_sha is not a commit sha`) is the
   designed state before step 1.
4. Still open elsewhere: `check-learnings` was red on pre-existing entries before this session (the four new entries
   follow the format but were not run through it); the E9 runbook's pre-flight block body still carries 3-handoff
   figures; the `[STRETCH]` partial-prefill experiment on E9's retained dumps.
