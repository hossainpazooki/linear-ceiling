# Handoff addendum — E7 skeleton instrument built (same evening as e7-rescope)

**Date:** 2026-09-01 (supersedes the re-verify lines of 2026-09-01-e7-rescope.md, whose
"E7 has ZERO code" and "expect 118 passed" were true at its write time and are now stale;
that brief stays unedited per convention.)
**Describes:** the working tree after the E7 skeleton build (the e7-rescope commit set is in
history; the skeleton set is pending the operator's commit).

## What now exists (built tonight, replay has NOT run)

- `config/e7.toml` — the registered parameters (entries 0006/0007) as committed config.
- `e7_traces` — normalized trajectory model + tau-bench adapter, grounded in the real
  artifact (inspected 2026-09-01): OpenAI-style message lists, **no timestamps, no usage
  fields, no per-step model identity**. Token counts are a chars/4 ESTIMATE — pluggable and
  **non-verdict-bearing until a successor entry registers the tokenizer** (open item #1).
- `e7_cost` — two-bound token/cost timeline (warm = incremental caching; cold = no-cache),
  per entry 0007's rule for timestamp-less traces.
- `e7_lanes` — Lane A distinguishes NOT-MEASURABLE (no/partial per-step metadata — never
  counted as zero) from a measured zero; Lane B is the registered two-tier cascade.
- `e7` — `assert_ready` gate (the enforcement entry 0006 registered): refuses until
  ledger + config/e7.toml are committed unmodified and entries 0006/0007 are in the
  committed ledger. **Refusal demonstrated live** before any trace was read
  (`E7 REFUSED: config/e7.toml is not committed as-is`, exit 2) — the E0 pattern repeated.
  Plus a skeleton driver: timelines + lanes + coverage check → `results/e7/skeleton_report.json`.
- `traces/` added to .gitignore; trajectories never enter history.

## Facts that constrain the day-2 gate plan

- tau-bench ships 4 files x 200 trajectories but only **2 distinct agents** (gpt-4o,
  sonnet-35-new) — it CANNOT meet entry 0007's >= 3-agents-per-suite floor alone; it ships
  as partial-with-coverage-stated, and **SWE-bench (many agents per split) must be the
  suite that clears the floor.**
- Lane A over tau-bench will be 100% not-measurable (run-level model only) — the expected
  premise finding, encoded as unmeasurable, not zero.

re-verify:
- `.venv/Scripts/python.exe -m pytest -q` -> 128 passed
- `.venv/Scripts/python.exe -m linear_ceiling.e7 --check` -> exit 2 with `E7 REFUSED` while
  the skeleton set is uncommitted; exit 0 after it lands
- `.venv/Scripts/python.exe -m linear_ceiling.ledger_check` / `lint_scope` / `seal verify` -> clean

## Open / next (in order)

1. Register the tokenizer method in a successor ledger entry before any number ships.
2. After the skeleton commits land: download tau-bench's 4 trajectory files into
   `traces/tau-bench/`, run `python -m linear_ceiling.e7`, inspect `skeleton_report.json`
   (day-2 gate items i + ii).
3. SWE-bench adapter (the floor-clearing suite) + the taxonomy event definitions (which need
   their own registration before frequencies ship).
4. Fail-closed E7 summarizer before the numbers-freeze gate.
