# Handoff — the n = 420 target dump and fit on the Algoverse box; 0033 and 0034 on the ledger; a shared-login incident

2026-09-09 (session `878feb6f`, driving the grant from home over JupyterHub). Newest commit this brief describes:
linear-ceiling `a71c3b4` = origin/main plus the uncommitted work in this brief's commit block. Upstream
`kv-transfer-replication` at `4633718` = origin, tree clean; its gitignored `data/kv/qwen3-0.6b-to-1.7b-n420/{source,target}`,
`mappers/qwen3-0.6b-to-1.7b/n420/`, `results/mapper/qwen3-0.6b-to-1.7b/n420/r2.json` and `results/probe/…` are the home
mirror. Supersedes the 2026-09-07 pick-up brief's block (a); block (b) stands.

## Current state

- **built / verified** — the n = 420 TARGET dump exists: 420 × 1,024 tokens, Qwen3-1.7B fp32 `sdpa_repeat_kv` at pin
  `223f469`, dumped on an H100 MIG 1g.20gb slice in 181 s, pulled and re-verified from raw bytes, nesting-checked
  against the CPU n = 50 half (≤ 1 fp16 ULP everywhere). Runbook: `docs/2026-09-08-n420-target-dump-runbook.md`.
  re-verify: `cd ~/dev/kv-transfer-replication && (cd data/kv/qwen3-0.6b-to-1.7b-n420/target && sha256sum -c --quiet ../n420_target.sha256 && echo 30/30)`.
- **built / verified** — the registered fit (`fit_mapper.py --k 1 4 8 --tag n420`) ran on the box (home swapped at k = 4:
  the pinned `build_features` is n_train × p fp32), outputs pulled and re-verified (3.05 GB, `box-logs-2026-09-08/fit_out.sha256`).
- **on the record** — 0033 (registration, appended 2026-09-08 with the two-provenance wording) and 0034 (figures, appended
  2026-09-09 by `append_0034.py`, both summarizers in-process, both passed; header dated 2026-09-08 = run date).
  re-verify: `grep -n "^### 003[34]" ledger/ledger.md`; `-m linear_ceiling.ledger_check` → `ledger ok`.
- **measured** — E9-long R2 ladder on the 1g.20gb slice: the 1.7B forward OOMs at T = 40,960 (`docs/probes/2026-09-08-e9-long-memory-ladder-1g20gb.out`);
  every seed option needs 3g.40gb or a full card. Row in the seed's §1.
- **INCIDENT** — the second box release (00:40Z 09-09) deleted `~/venv` + caches and stopped the server while a co-author's
  E9 audit (`~/e9-audit-data`, 8.9 GB, intact) was running under the same login; their process died, they restarted the
  server at 00:40:49Z. Protocol R7 now has a step 0 (foreign-presence abort); learnings entry 2026-09-09. The operator
  was told; the co-author has not been (not mine to reach).

## Locked decisions — premises checked

- Ruling (a) = re-dump: taken by the operator by requesting the box; premise held (target half never existed).
- The fit "at the pin", machine unspecified: the box run is legitimate; k = 1's four R² digits matched the home run's.
- 0034's date = run date (precedent 0020/0029/0031).

## Reuse map

- `tools/jupyterhub/` + session scratch scripts banked in the mirror's `box-logs-2026-09-08/` (`push_dumps.py` — one
  stream, 32 MB parts, `Connection: close`; `pull_fit.py`; `fit.sh` — needs `export PYTHONPATH=$PWD` on the box).
- `results/e8c/summary.md`, `results/e9c/summary.md` — 0034's tables, local by rule.

## Invariants (unchanged)

`results/`, `data/`, `traces/` never enter history; entries 0025–0034 immutable; no number enters the ledger or the
4-pager that a summarizer did not produce; upstream read-only (its gitignored data/mapper dirs are the mirror).

## Open / next

1. **Operator:** the commit block in the session log; tell the co-author about 00:40Z.
2. **R8 backup** of the n = 420 pair + tagged mapper to a private HF dataset needs a write token in the environment (none
   on this machine); layout: upstream's own (`data/kv/qwen3-0.6b-to-1.7b-n420/`, `mappers/qwen3-0.6b-to-1.7b/n420/`,
   `results/mapper/qwen3-0.6b-to-1.7b/n420/`).
3. Ruling (b) (§3.5 freeze: detached checkout at `d5786df` or a re-pin entry) still open; the E9-long seed's D1–D5 open,
   its YaRN commit must wait until nothing else needs the `223f469` invoked paths unchanged.
4. Freeze → 4-pager → PI review. `check-learnings` red on the same 7 pre-existing entries, 0 on today's.
