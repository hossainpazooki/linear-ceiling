# Handoff index

Pointers only — no evidence lives here. Entries are immutable: a later session writes a new
dated brief, never edits an old one. Run `/rigor:pickup` against the newest entry; it
re-verifies the brief's claims rather than trusting them.

| date | brief | describes commit | one-line |
|---|---|---|---|
| 2026-09-01 (latest, evening) | [e9-per-token-rule-0023](2026-09-01-e9-per-token-rule-0023.md) | `7fc1b5b` (+ this brief, README refresh, five learnings entries pending commit); upstream `36d73b3` | **E9's rule amended per token before any prefill (entry 0023); gate READY on the new pin.** Verdict statistic = median oracle selective-recompute fraction f*(τ_K), HOLDS ≤ 0.15 (CacheBlend) / DEGRADES ≥ 0.50 (operator's stated judgment); δ centered per layer-head in R²'s own units; τ_K = 1 − 0.6814 / τ_V = 1 − 0.5133 from the archive by `summarize_e9 --calibrate-tau` (median τ would have failed its own calibration at 6–7%); identity-halt + seeded δ_null controls, seam/depth profiles; upstream `--per-token` re-pin `36d73b3`; per-head-A5-vs-pooled discrepancy (0.6814 vs 0.6917) on the record. Suite 299, upstream 142, `e9 --check` ready. One process slip recorded: a mis-targeted `sed` in the handed-off commit block left `test_imports` red on main for one commit (fixed `7fc1b5b`). Verdict entry is **0024**. Next: request the A100 → runbook → `summarize_e9` → 0024. |
| 2026-09-02 | [verdicts-landed-e9-gated](2026-09-02-verdicts-landed-e9-gated.md) | `151bb719` + this brief and three learnings entries (pending commit) | **Every E7/E8 verdict on the record; E9 built, gated, READY — the program is blocked only on the A100 request.** Entries 0013–0020 chained: H-E7a `NOT CONFIRMED` (0.20% vs 10% after the 0017 instrument correction), H-E7b `UNESTIMABLE`, H-E8 `NOT CONFIRMED` (K UNRESOLVED / V DEGRADES at k=1), H-E9 registered with a frozen band. `upstream_gate` ancestor pins; reviewer-first README in the rigor format; staleness audit across living docs; GPU runbook. Suite 279, all gates green, `e8 --check` and `e9 --check` both print ready. Post-brief review (same day) added **entry 0021**: dating erratum (0016–0020 headings say 09-02 but were authored 09-01 — commit timestamps correct throughout) + Lane B disposed (descriptive counts `[BASELINE]`, 23,365 counterfactual boundaries vs 68 observed; dollar counterfactual `WITHDRAWN` per 0007's material-by-construction reasoning); README demotes the never-exercised seal and stops presenting E0's proxy verdict as a result. **Entry 0022** then records that 0009's SWE-bench calibration precondition ("SWE-bench requires its own calibration before its numbers ship") was not met by 0013/0015/0018 — with the bound (a ratio of same-counter counts is insensitive to uniform miscount) AND a measured sensitivity recomputed at append time: exact `o200k` (the true encoder for 64/68 o1-mini receivers) moves the H-E7a ratio 0.2030% → 0.2210%, ~45× under the cutoff; the miscalibration worked *against* the verdict. Repair deferred to an MLSys-cycle successor to 0009 (bundled with the gpt-4.1 item). H-E9's verdict entry was to be 0023; **entry 0023 (2026-09-01, later) instead amended E9's rule before any prefill** — per-token centered deviation + oracle selective-recompute fraction f*(τ_K) replace pooled R² as the verdict statistic (HOLDS ≤ 0.15 / DEGRADES ≥ 0.50, τ_K = 1 − 0.6814 from the archive), identity + δ_null controls and seam/depth profiles registered, upstream `--per-token` re-pin pending commit; suite 299. The verdict entry is now **0024**. Next: commit both repos (upstream first, record its sha in `config/e9.toml` + `UPSTREAM.md`) → A100 → runbook → `summarize_e9` → 0024. |
| 2026-09-01 | [e7-summarizer-all-corpora](2026-09-01-e7-summarizer-all-corpora.md) | working tree after `28c90d9` (pending commit) | **Numbers-freeze requirement BUILT: `summarize_e7` recomputes every E7 figure across all three corpora** — coverage/floors with 0011 units, unparsed set, every headroom row + aggregate, reported-usage validation, tokenizer + cost-basis labels — via one recursive comparator. All previously script-derived figures reproduce to the digit (68 switches, 0/68, 81.3%; +3,423 / −134) and three live tampers on the real report were refused by path. `e7_corpus` (one walk, all suites) + `e7_stats` (one pinned quantile convention). **Entry 0013** puts the headroom figures on the record; **entry 0014** registers the six-class invalidation taxonomy (detection + measurability per class) and fixes H-E7a's denominator (Lane A measurable subset) BEFORE any frequency is computed; `e7_taxonomy` built and wired, replay gated on the ledger commit. Recon: H-E7a ~1.4% vs 10% under the most generous denominator. Suite 230. GPU plan for LCFM in `docs/2026-09-01-lcfm-gpu-plan.md` (E8 = CPU; E9 = A100; 0009's "dumps gone" is wrong — they exist locally). **E8 BUILT and gated** (config/e8.toml, e8_text, e8, summarize_e8; upstream `score_mapper.py` reproduces the archived r2.json exactly); entries 0016/0017 drafted in `docs/drafts/`, band approved. **Entry 0015 WRITTEN from the first real taxonomy replay: H-E7a `NOT CONFIRMED` (1.41% vs 10%), H-E7b `UNESTIMABLE`, taxonomy frequencies `[BASELINE]`.** 0016 appended + upstream re-pinned `71df450`. **09-02 DEFECT: composio's second submission nests prompts as list nodes (30 files read as responses only) and headroom `paid` was the trajectory prefix, not the receiver's prefill — 0013 figures + 0015 ratio superseded by 0017, verdicts stand; corrected ratio 0.20% (0018).** All of 0015–0020 LANDED: taxonomy verdicts, 0016 E8 amendment, 0017 correction, 0018 corrected figures, 0019 E9 (H-E9 row added to the table + `REQUIRED_IDS`), 0020 **H-E8 NOT CONFIRMED** (K UNRESOLVED / V DEGRADES; two byte-identical runs). **E9 BUILT and gated** (`e9_align`/`e9`/`summarize_e9` + upstream `score_positions.py`, landed there as `7e41f792`; `upstream_gate` ancestor-pin so E8 survives the re-pin); live toy e2e on real models passed. Suite 279. Re-pin `7e41f792` DONE, `e9 --check` prints READY, GPU runbook written. Next: A100 request → E9 on the box → `summarize_e9` → 0021 (H-E9 verdict). NOTE: this brief's own re-verify lines predate E8/E9 (suite counts etc.) — the index row you are reading supersedes them; a fresh brief is due at the next /handoff. |
| 2026-09-01 | [e7-three-corpora-and-headroom](2026-09-01-e7-three-corpora-and-headroom.md) | `23441e7` + doc set (`28c90d9`) | **Three corpora adapted, coverage floor MET, first headroom numbers.** tau2-bench added as the second suite (800 trajs / 4 agents) and carries provider-reported usage — validating it exposed a **fixed ~3,240-token hidden prefix** public traces omit, so every trace-only cost figure is a lower bound (0012). 68 observed cross-model switches measured, **0/68 byte-identical**, headroom upper bound median 81.3% of paid. Entries 0010–0012 registered. Suite 183. **Headroom numbers are script-derived and may not enter the record until `summarize_e7` recomputes them.** |
| 2026-09-01 (latest) | [e7-instrument-and-recon](2026-09-01-e7-instrument-and-recon.md) | `62282e9` + uncommitted doc set | **E7 instrument built and run on tau-bench; no hypothesis decided.** Tokenizer registered with measured bias (exact where a public encoder exists, calibrated per content type otherwise); E8 registered, not run; fail-closed `summarize_e7` tamper-proven on the real corpus. SWE-bench recon: trajectories are in a public S3 bucket, 119/181 have them, ~68 structured — and **no format records a per-step model or timestamps**, so Lane A is unmeasurable there too. Suite 147. Supersedes the re-verify lines of both earlier 09-01 briefs. |
| 2026-09-01 (later) | [e7-skeleton](2026-09-01-e7-skeleton.md) | working tree after the E7 skeleton build (pending commit) | **E7 skeleton instrument built; replay has not run.** tau adapter + two-bound cost model + both lanes + `assert_ready` gate (refusal demonstrated live, exit 2). Suite 128. Supersedes e7-rescope's re-verify lines. tau-bench alone cannot meet the 3-agent floor; SWE-bench must clear it. Tokenizer still unregistered — successor entry before any number ships. |
| 2026-09-01 | [e7-rescope](2026-09-01-e7-rescope.md) | working tree after the 0007 review-amendment set (prior landed state `1f7ad90`) | **Screen line closed; E7 measurement program registered.** Entries 0006/0007: depth structure recorded via fail-closed summarizer, E7 thresholds + two-lane switch-point design + cost-model parameters pre-registered, `SHELVED` vocabulary + prior-entries-sha256 chain added to `ledger_check`. E7 has zero code; day-2 gate EOD 09-03 decides the LCFM sprint; MLSys 2027 (10-30) is the anchor. |
| 2026-08-26 | [w1-scaffold](2026-08-26-w1-scaffold.md) | `2361c72` (ledger + E0 rule only; everything else is uncommitted) | **W1 complete.** E0 ran on all six pairs — ladder verdict **SAME**, so G1 degrades to Variant 3 — with the rule verifiably committed before any weight was read; entry 0004 records it and H-S2's first clause is `NOT CONFIRMED`. Suite 108, all four gates green. Two caveats carried forward: nothing but `2361c72` is in history, and 0004 records the medians but not the per-layer structure (§2.3), which lives only in untracked `results/`. |
