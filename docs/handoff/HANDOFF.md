# Handoff index

Pointers only — no evidence lives here. Entries are immutable: a later session writes a new
dated brief, never edits an old one. Run `/rigor:pickup` against the newest entry; it
re-verifies the brief's claims rather than trusting them.

| date | brief | describes commit | one-line |
|---|---|---|---|
| 2026-09-04 (GPU day + verdict) | [e9-gpu-day-and-verdict](2026-09-04-e9-gpu-day-and-verdict.md) | working tree on `0a19b56` (0028/0029 + summarizer tolerance + probes uncommitted); upstream `d5786df` | E9 RAN on the Algoverse MIG slice after two refused attempts (OOM on f32 attention scores → 0026 re-pin; missing mapper → gate check); 0027 pre-prefill; 25/68 scored, 8 kept dumps home; summary refused on a same-machine tolerance → 0028; **H-E9 HELD (0029)**, f* = 0 on every handoff, cross arm beyond DEGRADES. |
| 2026-09-03 (latest) | [e-rl-design-and-0025](2026-09-03-e-rl-design-and-0025.md) | `5960c20` = origin/main, tree clean; upstream `36d73b3` | **E-RL designed (MLSys cost comparison, unregistered, no code); E9 entry 0025 on the record and the gate READY.** 0025: coverage 25/68 + comparison, τ_agent_K alongside, prefix-invariance HALT, causal seam, block lengths, bootstrap, τ ladder, keep n 8 (48 GB). README/CLAUDE.md/drafts README updated. Four learnings entries (stride-200 checkpoints; manifest-citation marker; keep redraw; identity-control tautology). Next: A100 request (human); E-RL rulings per design §8; then the upstream revision-aware `Pair`. |
| 2026-09-02 (0025 rebuilt) | (no brief; this row is the record; the review seed is the input) | working tree on `cb80ad0` = origin/main; upstream `36d73b3` dirty on four Run 8 files | **0025 was never appended: `cb80ad0` retired the script after its guard refused** (it read the prior keep n from HEAD, which the instrument commit `f8cecf7` had already moved to 8 — "nothing to register" — and the retire step ran anyway; learnings entry). Ledger has no 0025 anywhere; `e9 --check` REFUSES on origin as-is. Script restored from `f8cecf7` and EXTENDED with the independent review's findings 1–7, all descriptive, rule/τ_K/band untouched: coverage **25/68** registered with an included-vs-excluded comparison (|S| median 25,460 vs 52,141; 0018 overlap 0.985 vs 0.989); τ_agent_K = 1 − 0.5629 = 0.4371 alongside (K arms), recomputed from E8's report by `--calibrate-tau` (tau.json rewritten, ~2 min CPU); **prefix-invariance control that HALTS** (S vs S + R's first token, max centered δ ≤ 1e-4) beside the 0023 identity control, which loads one dump twice and cannot fail; causal b⁻(t) seam profile; matched-block lengths (real corpus: 155,257 matched tokens, 0.44% in length-1 blocks, 99.01% in blocks ≥ 4) + f* over blocks ≥ 4; seeded bootstrap of the median (reported, not read); δ_null equal-token fraction; `e9 --align-only` (ran: `results/e9/align/coverage.json`). Suite 380 + 1 skipped. Prior keep n now read from a pinned revision (`8b6cced`), not HEAD. Runbook still stale (other session's uncommitted edits; not touched). Next: commit instrument → run `append_0025.py` (16 s; runs `summarize_e7` in-process) → commit ledger alone → retire → `e9 --check` READY → A100. |
| 2026-09-02 (GPU pre-flight) | (no brief; this row is the record) | working tree on `8b6cced`; upstream `36d73b3` | **E9 GPU day pre-flighted on CPU; runbook amended; nothing run on a GPU.** Pick-up of the reviewer note: every re-verify line held (0/0 vs origin, manifest 188/188, real-corpus pin 5 passed, suite 356+1 skipped, `ledger_check`/`lint_scope` ok, `e9 --check` READY). Drift the brief did not know: `e8 --check` now REFUSES because the 0023 re-pin changed `scripts/score_mapper.py` + added `kvt/pertoken.py` (E8's invoked paths) — E8 is decided (0020) and the queued E8 amendment is the fix, not blocking E9. Read-only alignment recon over the real 68 handoffs: **25 included / 43 excluded**, 1,323,643 prefill tokens, max |S| 32,123, keep-subset dumps ≈ 14.3 GB. Toy dump → `score_positions --per-token` at the pin on the real models passed. Findings written into the runbook: the upstream runs **float32 and materializes full logits** (≈19.5 GB at 32k → peak ≈ 35 GB; the plan's "fits a 20 GB share" was wrong — the card must be exclusive, learnings entry); the cu121 wheel index tops out at torch 2.5.1 (install line fixed); Windows Git Bash has no rsync (tar-over-ssh form added); wall time ≈ 1–2 h not < 1 h; paid fallback (Lambda A100 40 GB $1.99/h, fetched 09-02) so the 09-04 go/no-go reads "rent", not "trace-only"; request text to paste. Next: submit the request (human-only). |
| 2026-09-02 (E-RL design) | [e-rl design](../2026-09-02-e-rl-design.md) (design doc, not a brief) | working tree on `8b6cced`; upstream `36d73b3` dirty on four Run 8 files | **E-RL (KV reuse across RL post-training checkpoints) DESIGNED, unregistered, no number; reframed the same day as an MLSys cost comparison on the operator's "not RL-algorithms" ruling.** Picked up from the τ seed; 0023 figures re-verified byte-identical; OLMo-2-0425-1B-RLVR1 checkpoints are stride **200** (card's "every 20" is template text), `main` ≠ `step_2600` and is not a lag point; base = `OLMo-2-0425-1B-DPO`. Citation set checked at source: AReaL recomputes on interruption (§4.1 quoted), vLLM `pause_generation(mode="keep")` + `clear_cache` with no guidance, Laminar/Stable Asynchrony/AIPO at summary level only. Two verdict statistics now: f*(τ_K) from 0023 and stale-vs-fresh importance ratio + ESS (promoted by named amendment; bounds are `???` — the anchors give an instrument, not a threshold). Own run (Qwen3-0.6B GRPO, per-step checkpoints, lags 1–8) primary and unconditional; OLMo descriptive tail. Upstream `Pair` has no revision field → revision-aware pair on an upstream branch is the first build step. Open: (B) bounds, three cell-table gaps, trainer pin, one-or-two H-ids, every seal number. Next: rulings on §8; A100 for E9 still first. |
| 2026-09-02 (latest) | [remote-state-and-local-data](2026-09-02-remote-state-and-local-data.md) | `bc5acbf` = origin/main; upstream `36d73b3` | **Reviewer note: everything produced is on origin (0/0); `traces/` and `results/` are local by rule and `config/e7-manifest.json` (188 sha256 + sizes, S3 keys) is the committed record.** Corpus emptied 09-02 03:11:58Z via a worktree junction (suspected), rebuilt byte-for-byte with `e7_manifest fetch` + LF-normalized tau clones, `check` 188/188; 0022 pin verified (0.2210%), exact-encoder request-level cold 10.62%; numbering provisional. Next: the A100. |
| 2026-09-02 (restored) | (no brief; this row is the record) | working tree on `0732a31` | **Traces restored and the 0022 pin VERIFIED.** `traces/` had been emptied at 03:11:58Z (learnings: worktree junction, mechanism suspected); rebuilt from the committed manifest by the new `e7_manifest fetch` (180 SWE-bench files from S3, sha256+size checked) and from fresh tau/tau2 clones after CRLF→LF normalization; `e7_manifest check` 188/188; E7 replayed against the committed config `d16cf4659aab` + manifest. `tests/test_e7_sensitivity.py::test_pin_composio_exact_reproduces_entry_0022` PASSES (565,025 / 255,690,850 = 0.2210%). The ruling's run done: `--strategy-override composio_swekit=exact,o4-mini*=exact,gpt-4.1*=exact` → registered 0.2210%, registered-warm 1.96%, request-level cold **10.62%** (from 10.00%), request-level warm 9.13%; tau2 overrides move no reading. `sensitivity` is in `results/e7/recon.json` (local); the paper clause is now "measured in recon.json pending registration". |
| 2026-09-02 (later) | (no brief; this row is the record) | working tree on `7bf9d8c` | **Ruling 2 was executed as ALLOCATION, not next-free: `13b8128` made `docs/drafts/README.md` the allocator and wrote "0025 = H-E9 verdict" into CLAUDE.md and the runbook.** Now resolved as PROVISIONAL (one sentence in the drafts README, the only allocator): numbers are assigned when a script is staged, in staging order; CLAUDE.md and the runbook point to the README with no number. Ruling 1 second half: `summarize_e7 --strategy-override AGENT=exact[,…]` built (fnmatch patterns; refuses a no-op; `sensitivity` in recon.json; config sha untouched) with synthetic tests and the real-corpus pin test `tests/test_e7_sensitivity.py::test_pin_composio_exact_reproduces_entry_0022` (0022 recounted the WHOLE composio family, so the agent-level pin is `composio_swekit=exact`; "o1-mini" is a receiver model, not an agent). NOT RUN on the real corpus: `traces/` is empty on this machine as of 2026-09-01T23:11 local, so the pin is unverified and the o4-mini / gpt-4.1* runs are pending. Do not read the self-appraisal as the tree. |
| 2026-09-02 | [track-b-merged-0024](2026-09-02-track-b-merged-0024.md) | `13b8128` = origin/main | **Track B on main; entry 0024 on the record; no verdict changes.** PR #1 merged the six items + marker fix; 0024 appended through the gated path (report byte-identical to the recon-only run) and cherry-picked; a stale squash was discarded. Recorded: request-level reading of the H-E7a denominator sits at 10.0012% (ruling needed, cell unchanged); SWE-bench subset = first-N of the S3 listing; CRLF config-sha defect fixed for E7/E9, E0/E8 still raw-bytes; #5 stopped on an upstream change. Next: the reading ruling, checkout cleanup, E0/E8 sha port. |
| 2026-09-01 late | [track-b-recon](2026-09-01-track-b-recon.md) | worktree `track-b-recon` on `f48b536`, uncommitted (per-item patches) | **Track B review items built, nothing appended.** #6 corpus manifest committed (188 files, 180 S3 key/ETag; driver, summarizer and lint refuse on disagreement) — selection rule RECOVERED: every SWE-bench submission's subset is the first N of the S3 listing = alphabetically-first instances; #1 `ledger_check` block diff vs base (trailing entry immutable) + verdict-cell provenance (`verdict:` lines from 0024), in CI; #2 overlap nulls (observed 0.988 vs same-family 0.498 / cross-family 0.386); #3 cache-aware readings — registered 0.20% / 1.82%, **request-level cold 10.0012% AT the cutoff** (independently recomputed), 0024 draft changes no cell and asks for a registration ruling; #4 scope wording landed; **#5 STOPPED** (pinned scorer rejects holdout 1.0 and writes no per-sequence moments — upstream change). Suite 331, gates green. Next: operator commits → gated driver rerun → 0023 → `docs/drafts/append_0024.py`. |
| 2026-09-01 (evening) | [e9-per-token-rule-0023](2026-09-01-e9-per-token-rule-0023.md) | `7fc1b5b` (+ this brief, README refresh, five learnings entries pending commit); upstream `36d73b3` | **E9's rule amended per token before any prefill (entry 0023); gate READY on the new pin.** Verdict statistic = median oracle selective-recompute fraction f*(τ_K), HOLDS ≤ 0.15 (CacheBlend) / DEGRADES ≥ 0.50 (operator's stated judgment); δ centered per layer-head in R²'s own units; τ_K = 1 − 0.6814 / τ_V = 1 − 0.5133 from the archive by `summarize_e9 --calibrate-tau` (median τ would have failed its own calibration at 6–7%); identity-halt + seeded δ_null controls, seam/depth profiles; upstream `--per-token` re-pin `36d73b3`; per-head-A5-vs-pooled discrepancy (0.6814 vs 0.6917) on the record. Suite 299, upstream 142, `e9 --check` ready. One process slip recorded: a mis-targeted `sed` in the handed-off commit block left `test_imports` red on main for one commit (fixed `7fc1b5b`). Verdict entry was written as 0024 in this brief; after Track B's 0024 landed it is **0025** per the drafts README, and E9's config sha was switched to the newline-normalized hash (Track B's CRLF fix) so a Linux-box report summarizes on a CRLF checkout. Next: request the A100 → runbook → `summarize_e9` → 0025. |
| 2026-09-02 | [verdicts-landed-e9-gated](2026-09-02-verdicts-landed-e9-gated.md) | `151bb719` + this brief and three learnings entries (pending commit) | **Every E7/E8 verdict on the record; E9 built, gated, READY — the program is blocked only on the A100 request.** Entries 0013–0020 chained: H-E7a `NOT CONFIRMED` (0.20% vs 10% after the 0017 instrument correction), H-E7b `UNESTIMABLE`, H-E8 `NOT CONFIRMED` (K UNRESOLVED / V DEGRADES at k=1), H-E9 registered with a frozen band. `upstream_gate` ancestor pins; reviewer-first README in the rigor format; staleness audit across living docs; GPU runbook. Suite 279, all gates green, `e8 --check` and `e9 --check` both print ready. Post-brief review (same day) added **entry 0021**: dating erratum (0016–0020 headings say 09-02 but were authored 09-01 — commit timestamps correct throughout) + Lane B disposed (descriptive counts `[BASELINE]`, 23,365 counterfactual boundaries vs 68 observed; dollar counterfactual `WITHDRAWN` per 0007's material-by-construction reasoning); README demotes the never-exercised seal and stops presenting E0's proxy verdict as a result. **Entry 0022** then records that 0009's SWE-bench calibration precondition ("SWE-bench requires its own calibration before its numbers ship") was not met by 0013/0015/0018 — with the bound (a ratio of same-counter counts is insensitive to uniform miscount) AND a measured sensitivity recomputed at append time: exact `o200k` (the true encoder for 64/68 o1-mini receivers) moves the H-E7a ratio 0.2030% → 0.2210%, ~45× under the cutoff; the miscalibration worked *against* the verdict. Repair deferred to an MLSys-cycle successor to 0009 (bundled with the gpt-4.1 item). H-E9's verdict entry was to be 0023; **entry 0023 (2026-09-01, later) instead amended E9's rule before any prefill** — per-token centered deviation + oracle selective-recompute fraction f*(τ_K) replace pooled R² as the verdict statistic (HOLDS ≤ 0.15 / DEGRADES ≥ 0.50, τ_K = 1 − 0.6814 from the archive), identity + δ_null controls and seam/depth profiles registered, upstream `--per-token` re-pin pending commit; suite 299. The verdict entry is now **0024**. Next: commit both repos (upstream first, record its sha in `config/e9.toml` + `UPSTREAM.md`) → A100 → runbook → `summarize_e9` → 0024. |
| 2026-09-01 | [e7-summarizer-all-corpora](2026-09-01-e7-summarizer-all-corpora.md) | working tree after `28c90d9` (pending commit) | **Numbers-freeze requirement BUILT: `summarize_e7` recomputes every E7 figure across all three corpora** — coverage/floors with 0011 units, unparsed set, every headroom row + aggregate, reported-usage validation, tokenizer + cost-basis labels — via one recursive comparator. All previously script-derived figures reproduce to the digit (68 switches, 0/68, 81.3%; +3,423 / −134) and three live tampers on the real report were refused by path. `e7_corpus` (one walk, all suites) + `e7_stats` (one pinned quantile convention). **Entry 0013** puts the headroom figures on the record; **entry 0014** registers the six-class invalidation taxonomy (detection + measurability per class) and fixes H-E7a's denominator (Lane A measurable subset) BEFORE any frequency is computed; `e7_taxonomy` built and wired, replay gated on the ledger commit. Recon: H-E7a ~1.4% vs 10% under the most generous denominator. Suite 230. GPU plan for LCFM in `docs/2026-09-01-lcfm-gpu-plan.md` (E8 = CPU; E9 = A100; 0009's "dumps gone" is wrong — they exist locally). **E8 BUILT and gated** (config/e8.toml, e8_text, e8, summarize_e8; upstream `score_mapper.py` reproduces the archived r2.json exactly); entries 0016/0017 drafted in `docs/drafts/`, band approved. **Entry 0015 WRITTEN from the first real taxonomy replay: H-E7a `NOT CONFIRMED` (1.41% vs 10%), H-E7b `UNESTIMABLE`, taxonomy frequencies `[BASELINE]`.** 0016 appended + upstream re-pinned `71df450`. **09-02 DEFECT: composio's second submission nests prompts as list nodes (30 files read as responses only) and headroom `paid` was the trajectory prefix, not the receiver's prefill — 0013 figures + 0015 ratio superseded by 0017, verdicts stand; corrected ratio 0.20% (0018).** All of 0015–0020 LANDED: taxonomy verdicts, 0016 E8 amendment, 0017 correction, 0018 corrected figures, 0019 E9 (H-E9 row added to the table + `REQUIRED_IDS`), 0020 **H-E8 NOT CONFIRMED** (K UNRESOLVED / V DEGRADES; two byte-identical runs). **E9 BUILT and gated** (`e9_align`/`e9`/`summarize_e9` + upstream `score_positions.py`, landed there as `7e41f792`; `upstream_gate` ancestor-pin so E8 survives the re-pin); live toy e2e on real models passed. Suite 279. Re-pin `7e41f792` DONE, `e9 --check` prints READY, GPU runbook written. Next: A100 request → E9 on the box → `summarize_e9` → 0021 (H-E9 verdict). NOTE: this brief's own re-verify lines predate E8/E9 (suite counts etc.) — the index row you are reading supersedes them; a fresh brief is due at the next /handoff. |
| 2026-09-01 | [e7-three-corpora-and-headroom](2026-09-01-e7-three-corpora-and-headroom.md) | `23441e7` + doc set (`28c90d9`) | **Three corpora adapted, coverage floor MET, first headroom numbers.** tau2-bench added as the second suite (800 trajs / 4 agents) and carries provider-reported usage — validating it exposed a **fixed ~3,240-token hidden prefix** public traces omit, so every trace-only cost figure is a lower bound (0012). 68 observed cross-model switches measured, **0/68 byte-identical**, headroom upper bound median 81.3% of paid. Entries 0010–0012 registered. Suite 183. **Headroom numbers are script-derived and may not enter the record until `summarize_e7` recomputes them.** |
| 2026-09-01 (latest) | [e7-instrument-and-recon](2026-09-01-e7-instrument-and-recon.md) | `62282e9` + uncommitted doc set | **E7 instrument built and run on tau-bench; no hypothesis decided.** Tokenizer registered with measured bias (exact where a public encoder exists, calibrated per content type otherwise); E8 registered, not run; fail-closed `summarize_e7` tamper-proven on the real corpus. SWE-bench recon: trajectories are in a public S3 bucket, 119/181 have them, ~68 structured — and **no format records a per-step model or timestamps**, so Lane A is unmeasurable there too. Suite 147. Supersedes the re-verify lines of both earlier 09-01 briefs. |
| 2026-09-01 (later) | [e7-skeleton](2026-09-01-e7-skeleton.md) | working tree after the E7 skeleton build (pending commit) | **E7 skeleton instrument built; replay has not run.** tau adapter + two-bound cost model + both lanes + `assert_ready` gate (refusal demonstrated live, exit 2). Suite 128. Supersedes e7-rescope's re-verify lines. tau-bench alone cannot meet the 3-agent floor; SWE-bench must clear it. Tokenizer still unregistered — successor entry before any number ships. |
| 2026-09-01 | [e7-rescope](2026-09-01-e7-rescope.md) | working tree after the 0007 review-amendment set (prior landed state `1f7ad90`) | **Screen line closed; E7 measurement program registered.** Entries 0006/0007: depth structure recorded via fail-closed summarizer, E7 thresholds + two-lane switch-point design + cost-model parameters pre-registered, `SHELVED` vocabulary + prior-entries-sha256 chain added to `ledger_check`. E7 has zero code; day-2 gate EOD 09-03 decides the LCFM sprint; MLSys 2027 (10-30) is the anchor. |
| 2026-08-26 | [w1-scaffold](2026-08-26-w1-scaffold.md) | `2361c72` (ledger + E0 rule only; everything else is uncommitted) | **W1 complete.** E0 ran on all six pairs — ladder verdict **SAME**, so G1 degrades to Variant 3 — with the rule verifiably committed before any weight was read; entry 0004 records it and H-S2's first clause is `NOT CONFIRMED`. Suite 108, all four gates green. Two caveats carried forward: nothing but `2361c72` is in history, and 0004 records the medians but not the per-layer structure (§2.3), which lives only in untracked `results/`. |
