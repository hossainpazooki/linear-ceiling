# Handoff — Track B merged to main; entry 0024 on the record; what the merge left open

**Date:** 2026-09-02
**Describes commit:** `13b8128379f99f8bdf1f2acc785a297f6c68ae26` = `origin/main` (pushed). Pick-up
measures drift from here. This brief and four learnings entries sit untracked until committed.
**Supersedes:** the re-verify lines of `2026-09-01-track-b-recon.md` (they described an uncommitted
worktree); that brief stays unedited and remains the design record of the six items.

## Current state

**built — Track B is on main.** PR #1 (`9440bbc`, a merge of `b44b4f6` into `ba35f7d`) landed the six
per-item commits plus the marker-fix commit; cherry-picks `c559e0e` (ledger 0024) and `264174e`
(drafts state) followed; Track A added `7093e55` (E9 config sha newline-normalized) and `13b8128`
(H-E9 verdict entry is 0025; handoff index correction). A local squash of the branch (`3e2a93`, on a
stale base) was discarded by `reset --hard origin/main`; it is not on any remote.
re-verify: `git log --oneline -6 origin/main` → the six commits above, newest first
re-verify: `git status --short` → empty (apart from untracked `.claude/` and the files this brief adds)

**built — entry 0024, through the gated path.** Appended in the `lc-track-b` checkout by
`docs/drafts/append_0024.py` after `e7.assert_ready` passed on the committed ledger, config and
manifest; the gated driver's report and `recon.json` were byte-identical to the recon-only run
that produced the preview. The script was deleted with its entry (drafts convention).
re-verify: `.venv/Scripts/python.exe -m linear_ceiling.ledger_check --against origin/main~4` → `ledger ok` (0024 is an append over `9440bbc`)
re-verify: `grep -c '^### 0024' ledger/ledger.md` → 1; `grep -c '10\.0012%' ledger/ledger.md` → 3
re-verify: `ls docs/drafts/` → `README.md` only

**built — the manifest, the lint gaps, the two recon flags, the wording** (design and figures:
`2026-09-01-track-b-recon.md`, entry 0024). Cross-track check after the merge: the CRLF config-sha
fix is now in E7 and E9; **E0 and E8 still hash raw bytes** (`e8.py:144`, `summarize_e8.py:49`).
re-verify: `grep -n 'sha256_file_bytes(cfg.config_path)' src/linear_ceiling/*.py` → e8.py and summarize_e8.py (and e0 via `config_sha256`)
re-verify: `.venv/Scripts/python.exe -m linear_ceiling.e7_manifest check` → `manifest ok: 188 files match disk` (needs local `traces/`)

**stopped — #5 E8 rescoring.** Pinned `score_mapper.py` refuses `--holdout-frac 1.0` (empty
training mask) and writes no per-sequence moments; upstream change required, so the seed's
0025/0026 cannot be registered as drafted. Nothing in E8 was changed by Track B.
re-verify: `grep -n 'empty training or held-out mask' ../kv-transfer-replication/scripts/score_mapper.py` → one line

**not started — the ruling on the denominator reading, the E8 upstream change, the cleanup of
the two Track B checkouts** (see Open / next).

## Locked decisions

- **Entry 0024 changes no verdict cell.** The request-level reading of "input-token spend" puts
  H-E7a's upper-bound ratio at 10.0012% (learnings 2026-09-01-request-level-reading-sits-on-the-cutoff);
  0006's wording admits both readings, so choosing one is a registration act for a successor to
  0006/0014, never a recomputation. Pick-up checks the reason: the registered reading (0014/0018) is
  still what the cell was decided under.
- **The H-E9 verdict entry is 0025** and must carry `verdict: H-E9 = <VERDICT>` (Track A's `13b8128`
  aligned every doc; `ledger_check` enforces the line).
- **Ledger history is linear on main; the squash was discarded.** The PR merge already contained
  the branch; a second copy of the same content would have duplicated it.
- **Text configs are hashed newline-normalized** (`hashing.sha256_text_file`); raw bytes stay only
  for downloaded/binary artifacts. Reason: a recorded provenance sha must be a property of the
  committed bytes, not of the checkout (learnings 2026-09-01-config-sha-was-crlf-dependent).
- **Git history is the operator's.** The rigor git-guard refused commits in the Track B session;
  the override was not taken. All Track B commits were made by the operator from per-item patches.

## Reuse map

- `docs/drafts/append_0024.py` is gone from the tree but lives at `git show c559e0e~1:docs/drafts/append_0024.py`
  — the ordering-guarded append pattern with `--preview` and `--number`; copy it for 0025+.
- `~/dev/linear-ceiling/.claude/track-b-patches/` — the six per-item patches, `resolve_markers.py`
  (structural marker resolution) and `swe_selection_recon.json`; untracked, delete when done.
- `ledger_check.check_against(now, base)` + `ledger_at(rev)`; `e7_manifest.list_s3` / `selection`;
  `e7_null.derangement`; `e7_cache.request_prompts`.

## Invariants

- An entry block committed at the push/PR base is byte-identical afterwards (CI runs
  `ledger_check --against` the base; an unreadable base fails).
- A non-`unresolved` cell has a named setter; from 0024 that setter is a `verdict:` line.
- Every E7 figure names its manifest sha; disk, report and manifest agree or E7 reads nothing.
- Registered entries are never edited: the wrong figure is superseded by a new entry.
- `git diff --check` before every `rebase --continue` (learnings 2026-09-02-rebase-continue-commits-conflict-markers):
  nothing in the repo refuses committed markers.

## Open / next

1. **Rule on the reading of "input-token spend"** (successor to 0006/0014). Until then H-E7a's
   `NOT CONFIRMED` stands as decided; 0024 has the request-level reading on the record as the one
   under which it would not, with the two bound directions that make the true value strictly lower.
2. **Delete the two Track B checkouts after inspecting both sides.** `~/dev/lc-track-b` (branch
   `track-b`, fully merged) and `.claude/worktrees/track-b-recon` (branch `worktree-track-b-recon`
   at `f48b536`, uncommitted copies of the merged work, five modified tracked files plus the new
   ones). Diff each modified file against `origin/main` first; every one should be a subset of
   what is merged.
   `git worktree remove ~/dev/lc-track-b && git branch -D track-b && git push origin --delete track-b`
   `git worktree remove --force .claude/worktrees/track-b-recon && git branch -D worktree-track-b-recon`
3. **E0/E8 config sha** still raw-bytes: port `sha256_text_file` (recorded values unchanged for LF).
4. **#5 needs an upstream change** (holdout 1.0 without a training mask; per-sequence SSE/SST),
   coordinated with Track A's pin; then 0025-style amendment → rerun → result entry.
5. **`upstream_gate.check_upstream` crashes from a worktree** (`NotADirectoryError`, because
   `../kv-transfer-replication` resolves relative to the worktree root); it should refuse with a message.
