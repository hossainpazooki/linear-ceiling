ts: 2026-09-09T00:45:00Z
commit: a71c3b4 (HEAD at the time; the runbook carrying the incident is uncommitted)
session: Claude Code session 878feb6f (n = 420 target dump + fit on the Algoverse grant)
status: verified
fact: A GPU grant login is a shared account, not an empty box. The R7 release checklist was executed twice on 2026-09-08/09; the second time, the sweep's deletions (`~/venv`, `~/.cache/pip`, `~/.cache/huggingface`, the clone) ran before its process listing, and the server stop that followed killed a co-author's E9 audit that was running under the same login (`~/e9-audit-data`, 8.9 GB, last write 00:34:52Z; a `bash -l` and a python under `~/venv` alive at 00:40:22Z). R7 step 5 says "stop every process of OURS" and step 2 says "delete nothing unverified", but nothing in R7 says "abort if the account holds anything that is not ours", and a one-liner that deletes first and lists second cannot honor either.
basis: The sweep's own output in the runbook (`docs/2026-09-08-n420-target-dump-runbook.md`, 00:40:22Z entry): the process listing printed `/bin/bash -l` and `/home/jupyter-rrhs-66f0/venv/bin/python …` AFTER the `rm -rf` lines in the same command; the subsequent read-only listing at 00:41:47Z shows the foreign files with their mtimes and the server `started: 2026-09-09T00:40:49Z` (restarted by the other user, my token mint had failed at that minute).
re-verify: grep -n "00:40:22Z" docs/2026-09-08-n420-target-dump-runbook.md; grep -n "step 5" docs/gpu-experiment-protocol.md   # R7 has no foreign-presence abort; this entry proposes one (R7 step 0: list ~ and `ps -u`, and abort the release if anything not ours is present or running)
